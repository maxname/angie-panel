server {
    listen 80;
    server_name parked.example.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    http2 on;
    server_name parked.example.com;
    ssl_certificate     $acme_cert_parked;
    ssl_certificate_key $acme_cert_key_parked;
    return 404;
}

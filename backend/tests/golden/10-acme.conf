# ACME clients + hidden per-certificate server blocks are generated here.
# M2: emit `acme_client <name> <directory-url> [challenge=...] ...;` per
# certificate, plus a hidden `server { server_name <domains>; acme <name>; }`
# block. For M1 (no issuance) this file is intentionally directive-free.
